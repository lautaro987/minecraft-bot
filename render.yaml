build:
  python:
    version: "3.11"
run:
  command: python main.py
services:
  - type: web
    name: minecraft-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
